# gyc web 实施计划（P0 地基 + P1 核心对话）

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent (recommended) or compose:execute to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `gyc --mini` 改造为 `gyc web` Web IDE —— 本计划实现 P0（webapp 脚手架 + 内嵌托管）与 P1（聊天核心，可"对话式编码"跑通）。

**Architecture:** 前端为仓库内一等 workspace 包 `src/webapp`（React + Vite + TS），复用 `@gyccode/protocol/v1` 生成的 hey-api 浏览器 SDK 做 REST/SSE/WS 调用；server 侧复用现有 `serveUIEffect` 内嵌 Web UI 机制（`opencode-web-ui.gen.ts` 清单 + dev 代理回退）。P1 聊天核心：`session.promptAsync` 发消息 + `global.event` SSE 订阅流式事件渲染。

**Tech Stack:** React 19, Vite, TypeScript, `@gyccode/protocol/v1`（hey-api gen client）, `@vitejs/plugin-react`。参考实现（实现时对照）：
- `E:\AI项目备份\Claude Code 源码资料\extracted-source\extracted-source\src\remote\SessionsWebSocket.ts`（WebSocket 会话订阅、auth 消息、心跳/重连）
- `...\src\bridge\replBridge.ts` / `bridgeMessaging.ts`（会话桥与消息协议）
- 本仓库 `src/gyccode/cli/cmd/run/stream.transport.ts`（mini 模式的全局事件订阅 + 单轮 prompt + idle 检测，P1 事件消费的直接参照）

---

## 阶段总览（完整路线图）

| 阶段 | 内容 | 产出 | 依赖 | 状态 |
|---|---|---|---|---|
| **P0** | webapp 脚手架 + 内嵌托管 + SDK 接入验证 | `gyc web` 打开 hello 页并能列出会话 | 无 | 本计划详述 |
| **P1** | 聊天核心：会话管理 + SSE 流式 + 工具审批 | 浏览器内"对话式编码"跑通 | P0 | 本计划详述 |
| P2 | IDE 增强：文件树 + monaco 编辑器/Diff + xterm 终端 | 完整四件套 | P0/P1 | 后续独立计划 |
| P3 | 命令改造：`gyc web` 主入口、`--mini` 废弃、文档/重连/性能 | 命令与打磨 | P0/P1 | 后续独立计划 |
| P4 | 云就绪：agent runtime 抽象边界文档化（不实现沙箱） | 抽象接口文档 | 全部 | 后续独立计划 |

> P2/P3/P4 各为一个独立计划（各自产出可运行软件），在 P0+P1 完成后按序展开。

---

# P0：webapp 脚手架 + 内嵌托管

### P0-Task 1: 创建 `src/webapp` workspace 包

**Files:**
- Create: `src/webapp/package.json`
- Create: `src/webapp/tsconfig.json`
- Create: `src/webapp/vite.config.ts`
- Create: `src/webapp/index.html`

- [ ] **Step 1: 创建 package.json**

`src/webapp/package.json`：
```json
{
  "name": "@gyccode/webapp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  }
}
```

- [ ] **Step 2: 创建独立 tsconfig（与根 tsconfig 的 solid jsxImportSource 隔离）**

`src/webapp/tsconfig.json`：
```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["vite/client"],
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src", "vite.config.ts"]
}
```

- [ ] **Step 3: 创建 vite 配置（dev 端口 8789 = server UI_UPSTREAM 默认值）**

`src/webapp/vite.config.ts`：
```ts
import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"

export default defineConfig({
  plugins: [react()],
  build: { outDir: "dist", target: "esnext" },
  server: { port: 8789, strictPort: true },
})
```

- [ ] **Step 4: 创建 index.html**

`src/webapp/index.html`：
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>gyc web</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: 提交**

```bash
git add src/webapp/package.json src/webapp/tsconfig.json src/webapp/vite.config.ts src/webapp/index.html
git commit -m "feat(webapp): scaffold src/webapp workspace package (React+Vite+TS)"
```

---

### P0-Task 2: 接线根 package.json / tsconfig

**Files:**
- Modify: `package.json`
- Modify: `tsconfig.json`

- [ ] **Step 1: 根 package.json 添加 React 依赖与 webapp workspace**

在根 `package.json` 的 `dependencies` 中新增：
```json
"react": "^19.0.0",
"react-dom": "^19.0.0"
```
在 `devDependencies` 中新增：
```json
"@types/react": "^19.0.0",
"@types/react-dom": "^19.0.0",
"@vitejs/plugin-react": "^4.3.0",
"vitest": "^3.0.0",
"jsdom": "^25.0.0",
"@testing-library/react": "^16.0.0",
"@testing-library/dom": "^10.0.0"
```
在 `workspaces` 数组中追加 `"src/webapp"`。

- [ ] **Step 2: 根 tsconfig 排除 webapp（避免 solid jsxImportSource 污染 React 编译）**

`tsconfig.json` 中 `include` 之后追加：
```json
"exclude": ["./src/webapp/**"]
```

- [ ] **Step 3: 创建 vitest 配置（jsdom 环境，独立于 bun test 的 solid 预载）**

`src/webapp/vitest.config.ts`：
```ts
import { defineConfig } from "vitest/config"
import react from "@vitejs/plugin-react"

export default defineConfig({
  plugins: [react()],
  test: { environment: "jsdom", globals: false },
})
```

> 说明：webapp 单测用 vitest（jsdom），与仓库既有 `bun test`（solid 预载）隔离，避免 tsconfig 的 solid jsxImportSource 干扰 React 测试。

- [ ] **Step 4: 安装依赖并验证**

Run: `bun install`
Expected: 成功，React 19 / vitest / testing-library 装入 node_modules。

- [ ] **Step 5: 提交**

```bash
git add package.json tsconfig.json src/webapp/vitest.config.ts bun.lock
git commit -m "chore(webapp): wire src/webapp workspace + react/vitest deps, exclude from solid tsconfig"
```

---

### P0-Task 3: hello 页面 + 生成 SDK 接入验证

**Covers:** [S2, S3]
<!-- 验证生成 SDK 在浏览器可用，hello 页拉到 session list，对齐 spec 二/三节 -->

**Files:**
- Create: `src/webapp/src/main.tsx`
- Create: `src/webapp/src/index.css`
- Create: `src/webapp/src/app/App.tsx`
- Test: `src/webapp/src/app/App.test.tsx`

- [ ] **Step 1: 写失败测试（App 渲染后请求 session list）**

`src/webapp/src/app/App.test.tsx`：
```tsx
import { describe, expect, it, vi } from "vitest"
import { render, screen } from "@testing-library/react"
import { App } from "./App"

vi.mock("@gyccode/protocol/v1", () => ({
  createGyccodeClient: () => ({
    session: {
      list: () => Promise.resolve({ data: [{ id: "sess-1" }, { id: "sess-2" }] }),
    },
  }),
}))

describe("App", () => {
  it("fetches and lists sessions", async () => {
    render(<App />)
    expect(await screen.findByText("sess-1")).toBeTruthy()
    expect(screen.getByText("sess-2")).toBeTruthy()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/app/App.test.tsx`
Expected: FAIL（App 尚未创建 / 依赖缺失）

- [ ] **Step 3: 写最小实现**

`src/webapp/src/main.tsx`：
```tsx
import { createRoot } from "react-dom/client"
import { App } from "./app/App"
import "./index.css"

createRoot(document.getElementById("root")!).render(<App />)
```

`src/webapp/src/index.css`：
```css
:root { color-scheme: dark; }
body { margin: 0; font-family: system-ui, sans-serif; background: #111; color: #eee; }
main { padding: 24px; max-width: 720px; margin: 0 auto; }
```

`src/webapp/src/app/App.tsx`：
```tsx
import { useEffect, useState } from "react"
import { createGyccodeClient } from "@gyccode/protocol/v1"

type Session = { id: string }

export function App() {
  const [sessions, setSessions] = useState<Session[]>([])
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    const client = createGyccodeClient({ baseUrl: "" })
    let cancelled = false
    client.session
      .list()
      .then((res) => {
        if (cancelled) return
        setSessions((res.data as Session[]) ?? [])
      })
      .catch((err: unknown) => {
        if (cancelled) return
        setError(err instanceof Error ? err.message : String(err))
      })
    return () => {
      cancelled = true
    }
  }, [])

  return (
    <main>
      <h1>gyc web</h1>
      {error ? <p style={{ color: "red" }}>连接失败: {error}</p> : null}
      <h2>会话列表</h2>
      {sessions.length === 0 && !error ? <p>（空，或尚未连接 server）</p> : null}
      <ul>{sessions.map((s) => <li key={s.id}>{s.id}</li>)}</ul>
    </main>
  )
}
```

> 注：若 `@gyccode/protocol/v1` 的 `session.list()` 返回与 `res.data` 形态不符，按 `src/protocol/v1/gen/types.gen.d.ts` 中 `SessionListResponses`（200: Array\<Session\>）校正类型断言。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/app/App.test.tsx`
Expected: PASS（2 条会话被渲染）

- [ ] **Step 5: 提交**

```bash
git add src/webapp/src
git commit -m "feat(webapp): hello page with session list via generated SDK"
```

---

### P0-Task 4: 内嵌 Web UI 清单生成 + server 托管

**Covers:** [S2, S3]
<!-- 复用现有 serveUIEffect/embeddedUI 内嵌机制，生成 opencode-web-ui.gen.ts 清单 -->

**Files:**
- Create: `src/gyccode/server/generated/opencode-web-ui.gen.ts`（占位清单，提交入库）
- Create: `scripts/build-webapp.mjs`（vite build + 生成真实清单）
- Modify: `src/gyccode/server/shared/ui.ts`（移除已可解析的 `@ts-expect-error`）
- Modify: `tsconfig.json`（`opencode-web-ui.gen.ts` 路径别名）
- Modify: `build.mjs`（接入 webapp 构建步骤）
- Modify: `package.json`（prepack 时构建 webapp；files 含 webapp dist）

- [ ] **Step 1: 创建占位清单（保证 tsc/build 总是可解析）**

`src/gyccode/server/generated/opencode-web-ui.gen.ts`：
```ts
// 由 scripts/build-webapp.mjs 在构建时覆盖为真实清单（URL 路径 → 磁盘绝对路径）。
// 占位空清单：embeddedUI() 返回 null → serveUIEffect 走 dev 代理回退。
export default {} as Record<string, string>
```

- [ ] **Step 2: tsconfig 添加路径别名**

`tsconfig.json` 的 `paths` 中追加（放 `paths` 顶部）：
```json
"opencode-web-ui.gen.ts": ["./src/gyccode/server/generated/opencode-web-ui.gen.ts"]
```

- [ ] **Step 3: 移除 shared/ui.ts 中已可解析的 @ts-expect-error**

`src/gyccode/server/shared/ui.ts` 的 `embeddedUI` 函数中，将：
```ts
    // @ts-expect-error - generated file at build time
    import("opencode-web-ui.gen.ts").then((module) => module.default as Record<string, string>).catch(() => null))
```
改为：
```ts
    import("opencode-web-ui.gen.ts").then((module) => module.default as Record<string, string>).catch(() => null))
```
（因 tsconfig 别名使模块可解析，`@ts-expect-error` 将变成未使用指令 TS2578，必须移除）

- [ ] **Step 4: 创建构建脚本（vite build + 生成清单）**

`scripts/build-webapp.mjs`：
```js
// 构建 src/webapp → dist，并生成内嵌 Web UI 清单 opencode-web-ui.gen.ts
// 清单键为相对路径（不带前导 /，如 "index.html"、"assets/foo.js"），
// 值为磁盘绝对路径；serveUIEffect 用 FSUtil.readFile 按需读取并加 CSP。
import { spawnSync } from "node:child_process"
import { readdirSync, statSync, writeFileSync, mkdirSync } from "node:fs"
import { join, resolve, relative } from "node:path"

const webappDir = resolve("src/webapp")
const distDir = join(webappDir, "dist")
const outFile = resolve("src/gyccode/server/generated/opencode-web-ui.gen.ts")

const viteBin = join(webappDir, "node_modules", ".bin", "vite")
const vite = spawnSync(process.execPath, [viteBin, "build"], {
  cwd: webappDir,
  stdio: "inherit",
  windowsHide: true,
})
if (vite.status !== 0) process.exit(vite.status ?? 1)

function walk(dir) {
  const out = {}
  for (const name of readdirSync(dir)) {
    const full = join(dir, name)
    if (statSync(full).isDirectory()) Object.assign(out, walk(full))
    else out[relative(distDir, full).split("\\").join("/")] = resolve(full)
  }
  return out
}

const manifest = walk(distDir)
mkdirSync(join(resolve("src/gyccode/server/generated")), { recursive: true })
writeFileSync(
  outFile,
  "// 由 scripts/build-webapp.mjs 生成，勿手改。\nexport default " + JSON.stringify(manifest, null, 2) + " as Record<string, string>\n",
)
console.log(`webapp manifest written: ${Object.keys(manifest).length} files -> ${outFile}`)
```

- [ ] **Step 5: build.mjs 接入 webapp 构建**

`build.mjs` 顶部（`gen-compose-bundle.mjs` 之后）追加：
```js
// 构建 webapp 并生成内嵌 Web UI 清单（serveUIEffect 托管）。GYCCODE_SKIP_WEBAPP=1 可跳过。
if (process.env.GYCCODE_SKIP_WEBAPP !== "1") {
  const web = spawnSync(process.execPath, ["scripts/build-webapp.mjs"], {
    stdio: "inherit",
    windowsHide: true,
  })
  if (web.status !== 0) process.exit(web.status ?? 1)
}
```

- [ ] **Step 6: package.json 的 scripts/files 补充**

- `scripts` 新增：`"webapp:build": "node scripts/build-webapp.mjs"`
- `scripts` 的 `"prepack"` 改为：`"prepack": "bun run webapp:build && bun run build"`
- `files` 追加：`"src/webapp/dist"`

- [ ] **Step 7: 验证 tsc + 单测 + 构建**

Run: `bun tsc --noEmit`
Expected: 0 错误（webapp 已被根 tsconfig exclude；shared/ui.ts 的 @ts-expect-error 已移除）

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/app/App.test.tsx`
Expected: PASS

Run: `node scripts/build-webapp.mjs`
Expected: `webapp manifest written: N files -> ...\opencode-web-ui.gen.ts`

- [ ] **Step 8: 提交**

```bash
git add src/gyccode/server/generated/opencode-web-ui.gen.ts scripts/build-webapp.mjs src/gyccode/server/shared/ui.ts tsconfig.json build.mjs package.json
git commit -m "feat(webapp): build pipeline generates embedded UI manifest; serve via existing serveUIEffect"
```

---

### P0-Task 5: 端到端验证 `gyc web` 托管 hello 页

**Covers:** [S2, S3, S6]
<!-- 验证 gyc web 打开后 hello 页可访问且能拉到会话列表 -->

**Files:**
- Test: `scripts/build-webapp.mjs` 产物 + `gyc web` 命令

- [ ] **Step 1: 全量构建**

Run: `bun build.mjs`
Expected: build done（node 目标 dist/、bun 目标 dist-bun/），webapp 清单生成无报错

- [ ] **Step 2: 启动 server 验证 hello 页**

Run（新终端）:
```bash
node bin/gyc web --port 4100 --no-open
```
Expected: 输出 `Local access: http://localhost:4100`

- [ ] **Step 3: curl 验证根路由返回 hello HTML**

Run: `curl -s http://localhost:4100/ | findstr "gyc web"`
Expected: 返回的 index.html 含 `gyc web` 标题与 `/assets/...`（构建后）

- [ ] **Step 4: 验证 hello 页能拉到会话（同源 API）**

Run: `curl -s http://localhost:4100/session`
Expected: 返回 JSON（可能为空数组 `[]` 或会话列表），非 401/404 —— 证明生成 SDK 同源调用链路可用

- [ ] **Step 5: 停止 server 并提交（若产物有改动）**

```bash
git add -A
git commit -m "verify(webapp): gyc web serves embedded hello page + /session reachable" || echo "nothing to commit"
```

---

# P1：聊天核心（会话管理 + SSE 流式 + 工具审批）

### P1-Task 1: 协议层封装（client hooks）

**Covers:** [S3, S5]
<!-- 封装生成 SDK 为类型化 hooks：useSessions / useEvents -->

**Files:**
- Create: `src/webapp/src/client/sdk.ts`
- Create: `src/webapp/src/client/useSessions.ts`
- Create: `src/webapp/src/client/useEvents.ts`
- Test: `src/webapp/src/client/useSessions.test.ts`

- [ ] **Step 1: 写失败测试（useSessions 返回会话列表）**

`src/webapp/src/client/useSessions.test.ts`：
```tsx
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { useSessions } from "./useSessions"

const list = vi.fn(() => Promise.resolve({ data: [{ id: "s1" }] }))
vi.mock("@gyccode/protocol/v1", () => ({
  createGyccodeClient: () => ({ session: { list } }),
}))

describe("useSessions", () => {
  it("loads sessions on mount", async () => {
    const { result } = renderHook(() => useSessions())
    await waitFor(() => expect(result.current.sessions).toHaveLength(1))
    expect(result.current.sessions[0].id).toBe("s1")
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useSessions.test.ts`
Expected: FAIL（useSessions 不存在）

- [ ] **Step 3: 写实现**

`src/webapp/src/client/sdk.ts`：
```ts
import { createGyccodeClient } from "@gyccode/protocol/v1"

// 同源访问（页面由 gyc server 托管）；跨源场景可用 baseUrl 指向远程 server。
export function sdk(directory?: string) {
  return createGyccodeClient({ baseUrl: "", directory })
}
```

`src/webapp/src/client/useSessions.ts`：
```ts
import { useCallback, useEffect, useState } from "react"
import { sdk } from "./sdk"

export type SessionItem = { id: string; title?: string | null }

export function useSessions() {
  const [sessions, setSessions] = useState<SessionItem[]>([])
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const reload = useCallback(async () => {
    setLoading(true)
    setError(null)
    try {
      const res = await sdk().session.list()
      setSessions((res.data as SessionItem[]) ?? [])
    } catch (err) {
      setError(err instanceof Error ? err.message : String(err))
    } finally {
      setLoading(false)
    }
  }, [])

  useEffect(() => {
    void reload()
  }, [reload])

  return { sessions, loading, error, reload }
}
```

`src/webapp/src/client/useEvents.ts`：
```ts
import { useEffect, useRef } from "react"
import { sdk } from "./sdk"

export type AnyEvent = { type: string; properties?: Record<string, unknown> }

// 订阅全局事件流（SSE）。事件经回调派发给 store reducer。
// 由生成 SDK 的 SSE 客户端负责断线重连（指数退避）。
export function useEvents(directory: string | undefined, onEvent: (e: AnyEvent) => void) {
  const cb = useRef(onEvent)
  cb.current = onEvent

  useEffect(() => {
    let disposed = false
    const client = sdk(directory)
    let sub: { stream: AsyncGenerator<AnyEvent> } | undefined

    void client.global
      .event({
        query: { directory },
        onSseEvent: (evt) => {
          if (!disposed && evt?.data) cb.current(evt.data as AnyEvent)
        },
      })
      .then((result) => {
        if (!disposed) sub = result
      })

    return () => {
      disposed = true
      void sub?.stream.return?.(undefined)
    }
  }, [directory])
}
```

> 注：`client.global.event` 返回 `Promise<ServerSentEventsResult>`，其 `.stream` 为 AsyncGenerator；若生成的 SSE 客户端在浏览器表现不符，回退方案是 `fetch("/event?directory=...")` 手动解析 `text/event-stream`（参考 `src/protocol/v1/gen/core/serverSentEvents.gen.js`）。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useSessions.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/webapp/src/client
git commit -m "feat(webapp): protocol hooks (useSessions/useEvents) over generated SDK"
```

---

### P1-Task 2: 聊天状态 store（消息 + 事件 reducer）

**Covers:** [S3, S5]
<!-- 消息列表 + 流式增量 + 事件 reducer，参考 mini 的 session-data/reduceSessionData -->

**Files:**
- Create: `src/webapp/src/state/chatReducer.ts`
- Test: `src/webapp/src/state/chatReducer.test.ts`

- [ ] **Step 1: 写失败测试（message.updated 更新消息、session.idle 置 idle）**

`src/webapp/src/state/chatReducer.test.ts`：
```ts
import { describe, expect, it } from "vitest"
import { chatReducer, initialChatState } from "./chatReducer"

describe("chatReducer", () => {
  it("upserts assistant message on message.updated", () => {
    const s = chatReducer(initialChatState(), {
      type: "message.updated",
      properties: { info: { id: "m1", role: "assistant" } } as never,
    })
    expect(s.messages.some((m) => m.id === "m1")).toBe(true)
  })

  it("marks session idle", () => {
    const s = chatReducer(initialChatState(), { type: "session.idle", properties: { sessionID: "s1" } })
    expect(s.idle).toBe(true)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/state/chatReducer.test.ts`
Expected: FAIL（chatReducer 不存在）

- [ ] **Step 3: 写实现**

`src/webapp/src/state/chatReducer.ts`：
```ts
export type ChatMessage = {
  id: string
  role: "user" | "assistant"
  parts: { type: string; text?: string; id?: string }[]
  error?: unknown
}

export type ChatState = {
  sessionID: string | null
  messages: ChatMessage[]
  idle: boolean
  busy: boolean
}

export const initialChatState = (): ChatState => ({ sessionID: null, messages: [], idle: true, busy: false })

type ChatAction =
  | { type: "message.updated"; properties: { info: ChatMessage } }
  | { type: "message.removed"; properties: { sessionID: string; messageID: string } }
  | { type: "session.idle"; properties: { sessionID: string } }
  | { type: "session.busy"; properties: { sessionID: string } }
  | { type: "hydrate"; messages: ChatMessage[]; sessionID: string }

export function chatReducer(state: ChatState, action: ChatAction): ChatState {
  switch (action.type) {
    case "hydrate":
      return { ...state, sessionID: action.sessionID, messages: action.messages, idle: true, busy: false }
    case "message.updated": {
      const info = action.properties.info
      const exists = state.messages.some((m) => m.id === info.id)
      const messages = exists ? state.messages.map((m) => (m.id === info.id ? info : m)) : [...state.messages, info]
      return { ...state, messages }
    }
    case "message.removed":
      return { ...state, messages: state.messages.filter((m) => m.id !== action.properties.messageID) }
    case "session.idle":
      return { ...state, idle: true, busy: false }
    case "session.busy":
      return { ...state, idle: false, busy: true }
    default:
      return state
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/state/chatReducer.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/webapp/src/state
git commit -m "feat(webapp): chat state reducer (message upsert, idle/busy)"
```

---

### P1-Task 3: 会话选择 + 消息加载（hydrate）

**Covers:** [S3, S5]
<!-- 选定会话后 session.messages 加载历史，然后订阅事件流增量更新 -->

**Files:**
- Create: `src/webapp/src/client/useChatSession.ts`
- Test: `src/webapp/src/client/useChatSession.test.ts`

- [ ] **Step 1: 写失败测试（选定会话后加载历史消息）**

`src/webapp/src/client/useChatSession.test.ts`：
```tsx
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { useChatSession } from "./useChatSession"

const messages = vi.fn(() => Promise.resolve({ data: [{ id: "m1", role: "assistant" }] }))
const list = vi.fn(() => Promise.resolve({ data: [{ id: "s1" }] }))
vi.mock("@gyccode/protocol/v1", () => ({
  createGyccodeClient: () => ({ session: { messages, list }, global: { event: () => Promise.resolve({}) } }),
}))

describe("useChatSession", () => {
  it("loads messages for selected session", async () => {
    const { result } = renderHook(() => useChatSession("s1"))
    await waitFor(() => expect(result.current.messages).toHaveLength(1))
    expect(messages).toHaveBeenCalledWith({ path: { id: "s1" } })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useChatSession.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`src/webapp/src/client/useChatSession.ts`：
```ts
import { useCallback, useEffect, useReducer, useRef } from "react"
import { chatReducer, initialChatState, type ChatMessage } from "../state/chatReducer"
import { useEvents, type AnyEvent } from "./useEvents"
import { sdk } from "./sdk"

// 选定会话：加载历史消息（hydrate），并订阅全局事件流做增量更新。
export function useChatSession(sessionID: string | null, directory?: string) {
  const [state, dispatch] = useReducer(chatReducer, undefined, initialChatState)
  const sessionRef = useRef(sessionID)
  sessionRef.current = sessionID

  const hydrate = useCallback(
    async (id: string) => {
      const res = await sdk(directory).session.messages({ path: { id } })
      const messages = (res.data as ChatMessage[]) ?? []
      dispatch({ type: "hydrate", sessionID: id, messages })
    },
    [directory],
  )

  useEffect(() => {
    if (!sessionID) {
      dispatch({ type: "hydrate", sessionID: "", messages: [] })
      return
    }
    void hydrate(sessionID).catch((err) => console.error("hydrate failed", err))
  }, [sessionID, hydrate])

  const onEvent = useCallback((e: AnyEvent) => {
    if (sessionRef.current && (e.type === "message.updated" || e.type === "message.removed")) {
      const props = e.properties as { sessionID?: string }
      if (props.sessionID && props.sessionID !== sessionRef.current) return
    }
    dispatch(e as never)
  }, [])

  useEvents(directory, onEvent)

  return state
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useChatSession.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/webapp/src/client/useChatSession.ts
git commit -m "feat(webapp): useChatSession hydrate + event-driven incremental updates"
```

---

### P1-Task 4: 消息输入 + 发送（promptAsync）

**Covers:** [S3, S5]
<!-- 发送消息走 session.promptAsync（非阻塞），靠事件流 idle 判定本轮回合结束，参考 stream.transport 的单轮模型 -->

**Files:**
- Create: `src/webapp/src/client/useSendPrompt.ts`
- Test: `src/webapp/src/client/useSendPrompt.test.ts`

- [ ] **Step 1: 写失败测试（发送文本 part 到当前会话）**

`src/webapp/src/client/useSendPrompt.test.ts`：
```ts
import { describe, expect, it, vi } from "vitest"
import { buildPromptParts } from "./useSendPrompt"

describe("buildPromptParts", () => {
  it("builds a text part for the given prompt", () => {
    const parts = buildPromptParts("你好")
    expect(parts).toEqual([{ type: "text", text: "你好" }])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useSendPrompt.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`src/webapp/src/client/useSendPrompt.ts`：
```ts
import { useCallback } from "react"
import { sdk } from "./sdk"

export type PromptPart = { type: "text"; text: string }

export function buildPromptParts(text: string): PromptPart[] {
  return [{ type: "text", text }]
}

export function useSendPrompt(sessionID: string | null, directory?: string) {
  const send = useCallback(
    async (text: string) => {
      if (!sessionID) throw new Error("未选择会话")
      const parts = buildPromptParts(text)
      await sdk(directory).session.promptAsync({
        path: { id: sessionID },
        body: { parts },
      })
    },
    [sessionID, directory],
  )
  return { send }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/client/useSendPrompt.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/webapp/src/client/useSendPrompt.ts
git commit -m "feat(webapp): send prompt via session.promptAsync (non-blocking)"
```

---

### P1-Task 5: 聊天 UI（会话列表 + 消息流 + 输入框）

**Covers:** [S3, S4, S5]
<!-- 三栏布局雏形：会话列表 / 消息流（流式渲染）/ 输入框，对齐 CC web 聊天面板 -->

**Files:**
- Create: `src/webapp/src/app/App.tsx`（改造为会话选择 + 聊天）
- Create: `src/webapp/src/app/SessionList.tsx`
- Create: `src/webapp/src/app/ChatPanel.tsx`
- Create: `src/webapp/src/app/MessageList.tsx`
- Create: `src/webapp/src/app/PromptInput.tsx`
- Test: `src/webapp/src/app/ChatPanel.test.tsx`

- [ ] **Step 1: 写失败测试（发送消息后调用 promptAsync）**

`src/webapp/src/app/ChatPanel.test.tsx`：
```tsx
import { describe, expect, it, vi } from "vitest"
import { fireEvent, render, screen } from "@testing-library/react"
import { ChatPanel } from "./ChatPanel"

const promptAsync = vi.fn(() => Promise.resolve({}))
vi.mock("@gyccode/protocol/v1", () => ({
  createGyccodeClient: () => ({ session: { promptAsync } }),
}))
vi.mock("../client/useChatSession", () => ({
  useChatSession: () => ({ messages: [], idle: true, busy: false }),
}))

describe("ChatPanel", () => {
  it("sends prompt on submit", () => {
    render(<ChatPanel sessionID="s1" />)
    const input = screen.getByPlaceholderText("输入消息…")
    fireEvent.change(input, { target: { value: "你好" } })
    fireEvent.keyDown(input, { key: "Enter" })
    expect(promptAsync).toHaveBeenCalledWith({
      path: { id: "s1" },
      body: { parts: [{ type: "text", text: "你好" }] },
    })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/app/ChatPanel.test.tsx`
Expected: FAIL

- [ ] **Step 3: 写实现**

`src/webapp/src/app/SessionList.tsx`：
```tsx
import type { SessionItem } from "../client/useSessions"

export function SessionList({
  sessions,
  selected,
  onSelect,
  onNew,
}: {
  sessions: SessionItem[]
  selected: string | null
  onSelect: (id: string) => void
  onNew: () => void
}) {
  return (
    <aside style={{ width: 220, borderRight: "1px solid #333", padding: 8 }}>
      <button onClick={onNew}>+ 新会话</button>
      <ul style={{ listStyle: "none", padding: 0 }}>
        {sessions.map((s) => (
          <li
            key={s.id}
            onClick={() => onSelect(s.id)}
            style={{ cursor: "pointer", padding: 4, background: s.id === selected ? "#2a2a2a" : "transparent" }}
          >
            {s.title ?? s.id.slice(0, 8)}
          </li>
        ))}
      </ul>
    </aside>
  )
}
```

`src/webapp/src/app/MessageList.tsx`：
```tsx
import type { ChatMessage } from "../state/chatReducer"

export function MessageList({ messages }: { messages: ChatMessage[] }) {
  return (
    <div style={{ flex: 1, overflowY: "auto", padding: 12 }}>
      {messages.map((m) => (
        <div key={m.id} style={{ marginBottom: 12 }}>
          <strong>{m.role === "user" ? "你" : "gyc"}</strong>
          <pre style={{ whiteSpace: "pre-wrap", fontFamily: "inherit" }}>
            {m.parts?.map((p) => (p.type === "text" ? p.text : `[${p.type}]`)).join("")}
          </pre>
          {m.error ? <p style={{ color: "red" }}>错误: {String(m.error)}</p> : null}
        </div>
      ))}
    </div>
  )
}
```

`src/webapp/src/app/PromptInput.tsx`：
```tsx
import { useState } from "react"

export function PromptInput({ disabled, onSubmit }: { disabled: boolean; onSubmit: (text: string) => void }) {
  const [value, setValue] = useState("")
  return (
    <input
      placeholder="输入消息…"
      value={value}
      disabled={disabled}
      onChange={(e) => setValue(e.target.value)}
      onKeyDown={(e) => {
        if (e.key === "Enter" && value.trim()) {
          onSubmit(value.trim())
          setValue("")
        }
      }}
      style={{ padding: 10, fontSize: 14 }}
    />
  )
}
```

`src/webapp/src/app/ChatPanel.tsx`：
```tsx
import { useState } from "react"
import { useChatSession } from "../client/useChatSession"
import { useSendPrompt } from "../client/useSendPrompt"
import { MessageList } from "./MessageList"
import { PromptInput } from "./PromptInput"

export function ChatPanel({ sessionID }: { sessionID: string }) {
  const { messages, busy } = useChatSession(sessionID)
  const { send } = useSendPrompt(sessionID)
  const [sendError, setSendError] = useState<string | null>(null)

  return (
    <section style={{ display: "flex", flexDirection: "column", flex: 1, minWidth: 0 }}>
      <MessageList messages={messages} />
      {sendError ? <p style={{ color: "red" }}>{sendError}</p> : null}
      <PromptInput
        disabled={busy}
        onSubmit={(text) => {
          send(text).catch((err) => setSendError(err instanceof Error ? err.message : String(err)))
        }}
      />
    </section>
  )
}
```

改造 `src/webapp/src/app/App.tsx`（会话选择 + 新建会话 + 聊天）：
```tsx
import { useState } from "react"
import { useSessions } from "../client/useSessions"
import { sdk } from "../client/sdk"
import { SessionList } from "./SessionList"
import { ChatPanel } from "./ChatPanel"

export function App() {
  const { sessions, reload } = useSessions()
  const [selected, setSelected] = useState<string | null>(null)

  const onNew = async () => {
    const res = await sdk().session.create({ body: {} })
    const created = (res.data as { id: string } | undefined)?.id
    if (created) {
      setSelected(created)
      await reload()
    }
  }

  return (
    <main style={{ display: "flex", height: "100vh", margin: 0 }}>
      <SessionList sessions={sessions} selected={selected} onSelect={setSelected} onNew={onNew} />
      {selected ? <ChatPanel sessionID={selected} /> : <div style={{ flex: 1, padding: 24 }}>选择或新建一个会话</div>}
    </main>
  )
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/app/ChatPanel.test.tsx`
Expected: PASS

- [ ] **Step 5: 验证 `bun tsc --noEmit`（webapp 目录用其自身 tsconfig）**

Run: `bunx tsc -p src/webapp/tsconfig.json --noEmit`
Expected: 0 错误

- [ ] **Step 6: 提交**

```bash
git add src/webapp/src/app
git commit -m "feat(webapp): chat UI (session list + streaming message list + prompt input)"
```

---

### P1-Task 6: 工具审批卡片（permission 事件 → 交互确认）

**Covers:** [S3, S5, S7]
<!-- agent 请求工具时 permission.requested 事件 → 审批卡片 → permission.resolve；对齐 CC web 权限审批交互 -->

**Files:**
- Create: `src/webapp/src/state/permissionReducer.ts`
- Create: `src/webapp/src/app/PermissionCard.tsx`
- Create: `src/webapp/src/client/usePermissions.ts`
- Test: `src/webapp/src/state/permissionReducer.test.ts`

- [ ] **Step 1: 写失败测试（permission.requested 入队、resolve 移除）**

`src/webapp/src/state/permissionReducer.test.ts`：
```ts
import { describe, expect, it } from "vitest"
import { permissionReducer, initialPermissionState } from "./permissionReducer"

describe("permissionReducer", () => {
  it("queues a permission request", () => {
    const s = permissionReducer(initialPermissionState(), {
      type: "permission.requested",
      properties: { permissionID: "p1", title: "运行命令", tool: "bash", input: {} } as never,
    })
    expect(s.queue).toHaveLength(1)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/state/permissionReducer.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`src/webapp/src/state/permissionReducer.ts`：
```ts
export type PermissionItem = {
  permissionID: string
  title: string
  tool: string
  input: Record<string, unknown>
}

export type PermissionState = { queue: PermissionItem[] }

export const initialPermissionState = (): PermissionState => ({ queue: [] })

export function permissionReducer(state: PermissionState, action: { type: string; properties: Record<string, unknown> }): PermissionState {
  switch (action.type) {
    case "permission.requested":
      return { queue: [...state.queue, action.properties as unknown as PermissionItem] }
    case "permission.resolved":
      return { queue: state.queue.filter((p) => p.permissionID !== (action.properties.permissionID as string)) }
    default:
      return state
  }
}
```

`src/webapp/src/app/PermissionCard.tsx`：
```tsx
import type { PermissionItem } from "../state/permissionReducer"

export function PermissionCard({
  item,
  onResolve,
}: {
  item: PermissionItem
  onResolve: (permissionID: string, allow: boolean) => void
}) {
  return (
    <div style={{ border: "1px solid #555", borderRadius: 8, padding: 12, margin: 8 }}>
      <strong>{item.title}</strong>
      <p>
        工具: <code>{item.tool}</code>
      </p>
      <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify(item.input, null, 2)}</pre>
      <button onClick={() => onResolve(item.permissionID, true)}>允许</button>
      <button onClick={() => onResolve(item.permissionID, false)}>拒绝</button>
    </div>
  )
}
```

`src/webapp/src/client/usePermissions.ts`：
```ts
import { useCallback, useReducer } from "react"
import { initialPermissionState, permissionReducer, type PermissionItem } from "../state/permissionReducer"
import { useEvents, type AnyEvent } from "./useEvents"
import { sdk } from "./sdk"

// 订阅 permission.* 事件维护审批队列；resolve 走 session.postSessionIdPermissionsPermissionId。
// 端点: POST /session/{id}/permissions/{permissionID}，body: { response: "once"|"always"|"reject" }
export function usePermissions(sessionID: string, directory?: string) {
  const [state, dispatch] = useReducer(permissionReducer, undefined, initialPermissionState)

  useEvents(directory, (e: AnyEvent) => {
    if (e.type === "permission.requested" || e.type === "permission.resolved") dispatch(e as never)
  })

  const resolve = useCallback(
    async (permissionID: string, allow: boolean) => {
      await sdk(directory).session.postSessionIdPermissionsPermissionId({
        path: { id: sessionID, permissionID },
        body: { response: allow ? "once" : "reject" },
      })
      dispatch({ type: "permission.resolved", properties: { permissionID } })
    },
    [sessionID, directory],
  )

  return { queue: state.queue as PermissionItem[], resolve }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run --config src/webapp/vitest.config.ts src/webapp/src/state/permissionReducer.test.ts`
Expected: PASS

- [ ] **Step 5: 在 ChatPanel 挂载 PermissionCard（消息流上方渲染审批队列）**

修改 `src/webapp/src/app/ChatPanel.tsx`：引入 `usePermissions(sessionID)`，在 `MessageList` 上方渲染 `queue.map((p) => <PermissionCard key={p.permissionID} item={p} onResolve={resolve} />)`。

> 注：`permission.requested` 事件属性名（`permissionID`/`title`/`tool`/`input`）以实际事件载荷为准，若字段名不同按 `src/gyccode/event-manifest.ts` 中 `PermissionRequested` 定义校正 reducer 的字段映射。

- [ ] **Step 6: 提交**

```bash
git add src/webapp/src/state/permissionReducer.ts src/webapp/src/app/PermissionCard.tsx src/webapp/src/client/usePermissions.ts
git commit -m "feat(webapp): tool permission approval cards (permission.requested -> resolve)"
```

---

### P1-Task 7: P1 端到端验收（浏览器对话式编码）

**Covers:** [S2, S3, S6]
<!-- 浏览器内真实对话一轮：发消息 → 流式回复 → idle；通过后进入 P2 -->

**Files:**
- Test: `gyc web` + 浏览器/curl 手动验证

- [ ] **Step 1: 全量构建**

Run: `bun build.mjs`
Expected: build done，webapp 清单生成

- [ ] **Step 2: 启动 server**

Run（新终端）:
```bash
node bin/gyc web --port 4100 --no-open
```
Expected: `Local access: http://localhost:4100`

- [ ] **Step 3: 浏览器验证四件套中的聊天闭环**

打开 `http://localhost:4100`，完成：新建会话 → 输入"你好" → Enter → 看到 gyc 流式回复 → 状态回 idle。同时验证会话列表能列出该会话。

- [ ] **Step 4: 回归后端测试**

Run: `bun test --preload ./scripts/bun-solid-preload.ts`
Expected: 全部既有测试仍通过（webapp 改动不影响 server）

- [ ] **Step 5: 提交（若修复了过程中的问题）**

```bash
git add -A
git commit -m "verify(webapp): P1 conversational coding loop works end-to-end in browser" || echo "nothing to commit"
```

---

## 后续阶段（P2/P3/P4，独立计划）

- **P2（IDE 增强）**：文件树（`file.list`/`file.status` + git 状态）、monaco 编辑器（只读/编辑 + `session.diff` 渲染 Diff）、xterm 终端（`pty.create` → connect token → WS `pty.connect`）。产出：浏览器内完整四件套。
- **P3（命令与打磨）**：`gyc web` 主入口改造（`--mini` 废弃提示 + 迁移引导）、README/帮助文档、SSE/WS 断线重连与错误恢复打磨、消息列表虚拟化（react-virtuoso）、monaco 懒加载与性能优化。
- **P4（云就绪）**：agent runtime 抽象边界文档化（InstanceContext 已有支撑），为 v2 远程沙箱预留接口；不实现沙箱。

> 每个阶段为独立计划，均在 P0+P1 落地后按序创建并执行。



