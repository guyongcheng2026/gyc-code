# gyc-code 14 维度全面审查与修复（2026-08-17）

> 对应 commit：`d312402`（10 文件，全量测试 496 pass / 7 存量失败）

## 一、审查范围与结论

按 14 个维度（重点：稳定性、安全性、硬盘发烫/频繁写盘、LLM 延时、CH 缓存命中率）对 gyc-code 全项目扫描取证：

- **P0×1**：cron 任务无触发循环（`schedule_cron` 承诺调度但永不执行）
- **P1×4**：技能仓库远程 index 路径穿越任意写、TUI 渲染线程 execSync 阻塞（最坏 15s 冻结）、cronToHuman 单值误显 `*`、server 无限流
- **P2×6**：auth_token 走 URL query、非常量时间口令比较、cron dom/dow AND 语义、$EDITOR 解析、`2>nul` POSIX 兼容、`-z` rename 解析
- **良好设计确认**：日志批量+节流+轮转（磁盘噪声专项）、prompt 三层分片缓存 + 日期/记忆注入 user tail（CH 前缀字节稳定）、flock 心跳 1s 下限、16ms 事件批渲染

## 二、修复清单（已全部落地）

| 优先级 | 问题 | 修复位置 |
|---|---|---|
| P0 | cron 永不触发 | `cron.ts`（CronTask+sessionID、markFired）+ `prompt.ts`（30s forkScoped 触发循环，会话失效自动清理） |
| P1 | 路径穿越 | `gyccode/skill/discovery.ts` 移植 v2 版 isSafeSegment/isSafeRelativePath |
| P1 | 渲染阻塞 | 4 个 dialog 全部 execSync→execFile 异步 + createResource |
| P1 | cronToHuman | fmtField 显式 min/max 判满区间 |
| P2 | 凭据泄漏 | authorization.ts：query 凭据仅限 WebSocket upgrade |
| P2 | 计时侧信道 | auth.ts：sha256+timingSafeEqual |
| P2 | cron 语义 | matchesCron dom/dow 改标准 OR |
| P2 | $EDITOR | 带引号解析 + 去 shell:true + cmd shim 降级重试 |
| P2 | 2>nul | dialog-upgrade 改参数数组直传 |
| P2 | rename 解析 | dialog-commit 消费第二个 NUL 段（`old -> new`） |

## 三、撤回与教训

- **RcMap `idleTimeToLive: 0` 误判**：effect v4 语义是"引用归零后经该时长释放"，0=立即回收，**无泄漏**。教训：对第三方 API 语义存疑时先读 `node_modules/**/*.d.ts` 原文，不要按字面臆断。
- **worklog 重复条目**：钩子已同步后又手动补跑 `worklog-sync.mjs`，同 hash 幂等去重失效（commit message 多行时 hash 匹配可能受影响），已手工去重。教训：钩子输出不可见时先查流水文件再决定是否补跑。

## 四、验证

- lint：10 文件 0 错误
- 冒烟：`cronToHuman('30 14 28 2 *')` → `30 14 28 2 *`（修复前 `* 14 28 2 *`）；`0 0 1 * 1` 下次触发 2026-08-24 周一（OR 语义 ✓）
- 全量 `bun test`：496 pass；7 fail 经 stash 基线对比确认为存量（webapp 测试需 vitest DOM 环境，`test` script 本就排除）

## 五、待办

- [ ] server 限流中间件（令牌桶 + 每用户并发信号量 + 上游 429 熔断，需与 HttpEffect 管道设计对齐）
- [ ] dist/ 279 文件（含 mp3/wasm）入库清理
- [ ] worklog-sync.mjs 幂等去重修复（多行 commit message 场景）
