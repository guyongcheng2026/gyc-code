- Search the web using the session's web search provider — real-time search with optional URL scraping; returns content from the most relevant sites.
- Use for up-to-date information beyond your knowledge cutoff; searches run automatically within a single API call.

Usage notes:
  - Live crawling modes when available: 'fallback' (backup if cached unavailable) or 'preferred' (prioritize live crawling); search types: 'auto' (balanced), 'fast' (quick), 'deep' (comprehensive); configurable context length and domain filtering

The current year is {{year}}. You MUST use this year when searching for recent information or current events
- Example: if the current year is 2026 and the user asks for "latest AI news", search for "AI news 2026", NOT "AI news 2025"
