# 2026-08-10 gyc-code 工作记录

> 类型：子代理无限空转死循环排查与修复（会话窗口失去响应）
> 关联：[[2001.我的助手工具链/gyc-code-项目架构|gyc-code 项目架构]]
> 仓库：本地 `C:\Users\谷勇成\gyc-cli` / GitHub `guyongcheng2026/gyc-code`（commit `08c6e57`）

---

## 一、现象

- 5:37 启动的 gyc 会话窗口「失去响应」，无法输入与发送内容
- 进程未死（Responding=True），但 CPU/内存持续增长（bun PID 8140：CPU 累计 3427 秒、内存 914MB+）

## 二、排查结论

- 主会话（`ses_01789a...`）跑 compose「代码质量审查」流程已 3.5 小时（step 72+，派发 20+ 子代理）
- TUI 在会话 busy 时禁用输入框（`src/tui/ui/dialog-prompt.tsx` `!props.busy`），这是设计行为
- 根因 1：`deriveSubagentSessionPermission` 用「是否显式声明 task 规则」判断子代理能力；general 用通配 `*: allow`，被误判为无 task 权限 → 强制注入 `task deny`
- 根因 2：general 子代理未配置 `steps`，`agent.steps ?? Infinity` 无限步
- 结果：子代理每轮调模型都想用工具 → 全部被拒 → 再调，空转 36 步/10 分钟才退出；任务链一环接一环，主会话长期 busy

## 三、修复（commit `08c6e57`）

1. `src/gyccode/agent/subagent-permissions.ts`：新增 `permits()`，按匹配顺序取最后一条规则（显式同名或通配 `*`），非 deny 即视为具备权限 → general 恢复 task 权限，explore 保持只读
2. `src/gyccode/session/prompt.ts`：subagent 未配置 steps 时默认 20 步上限（`SUBAGENT_MAX_STEPS`）
3. `src/gyccode/session/prompt.ts`：连续 10 步纯工具调用（无可见文本输出）快速失败（`MAX_CONSECUTIVE_TOOL_ONLY_STEPS`），标记错误并结束

## 四、验证

- `bun run build` 通过
- 内联单测：general → 仅 todowrite deny（不再注入 task deny）；explore → todowrite + task deny（保持只读）
- 已推送 GitHub（`08c6e57`）

## 五、恢复操作（旧会话）

- 按 `Esc` 中断当前会话，或 `taskkill /PID 11020 /T /F` 结束旧实例
- 修复需重新启动 gyc 生效
---

## 六、后续修复：空转保护误杀正常流程（2026-08-10 下午）

> 类型：08c6e57 引入的空转判定过激，误杀正常编码流程

### 现象
- gyc cli 运行几分钟后频繁中断并提示：`Repeated tool calls produced no visible progress after 10 steps`（所有模型均出现）

### 排查结论
- 解析本地会话库（`gyccode-local.db`）4 个真实触发实例，错误轮前连续 10 轮均为「reasoning + 工具调用」且工具全部 completed 成功
- 根因：08c6e57 判定 `isToolOnlyStep = finish==="tool-calls" && 无 text part`。Compose/DeepSeek 等模型工具轮只输出 reasoning 不输出 text，被误判为「无可见进展」
- 仅触发 4 中有一例 grep 工具失败（Ripgrep JSON 超 64KB），属工具边界错误，非空转

### 修复（工作区已改，待提交）
1. 新增 `src/gyccode/session/tool-stall.ts`：空转判定纯函数 —— 无可见文本 +（工具失败/未完成 **或** 与最近 20 轮工具签名完全重复）才计为空转
2. `src/gyccode/session/prompt.ts`：接入新判定；触发前记录 warning 日志；错误消息用实际阈值
3. `src/core/v1/config/config.ts`：新增 `llm.max_consecutive_tool_only_steps`（默认 10，0=关闭）
4. 新增 `tool-stall.test.ts`：10 项单测覆盖误杀场景与真空转场景

### 验证
- `bun run build` 通过；`tsc --noEmit` 通过
- 相关单测 32 项全过（tool-stall 10 项）
- 4 个真实触发会话回放：新逻辑全部不再误触发
- 模拟真空转（10 轮全失败/pending/完全重复）仍能触发中断；9 轮失败+1 轮成功不触发

### 待办
- 工作区存在其他会话进行中改动（thinking-keywords、compaction 重构、dedupe、llm-timeout 等），与 prompt.ts/config.ts 交织，本次修复未单独提交，待谷总确认提交策略
