# gyc-code 2026-08-20 工作记录

> 主题：gyc web 页面空白根因修复（内嵌 UI 清单 hash 过期 → 静态资源回退 index.html）
---

## 一、现象（谷总反馈）
- http://127.0.0.1:4096/ 启动后页面空白

## 二、排查与根因
1. 首页 HTML 正常返回（200），但 `/assets/index-*.js`、`.css` 请求返回的是 index.html（Content-Type: text/html，395 字节）
2. 服务进程 `node dist/index.js web`（PID 6772），dist 构建于 08-19 08:57
3. 根因：`scripts/build-webapp.mjs` 生成的 `src/gyccode/server/generated/opencode-web-ui.gen.ts` 清单在 dist 构建时嵌入旧 hash（index-CeEt578Z.js / index-CbiSdrM7.css），而 src/webapp/dist 在 08-19 16:53 重新构建后入口 hash 已变（index-BVSD271u.js / index-CTVbJ6kK.css）；`serveEmbeddedUIEffect`（shared/ui.ts）按清单 key 匹配，找不到资源时回退 index.html → JS/CSS 加载失败 → 空白页

## 三、修复
- `bun run build` 重建 webapp + 重新生成 opencode-web-ui.gen.ts（97 个文件）并重新打包 dist
- 重启 gyc web（node dist/index.js web）

## 四、验证
- 首页 200，引用新入口 /assets/index-B4Bbcjb-.js，返回 text/javascript 867KB（真实内容）
- CSS /assets/index-CTVbJ6kK.css 返回 text/css 47KB
- 同步：commit 044802e + push origin main（gh-proxy.com 代理），本地与远程 HEAD 一致

---

## 二、gyc web 对齐 DSH 第五轮（2026-08-20 晚）

- **真实可用性修复**：
  - 命令选择失效：PromptInput 三处根因（currentCmd 取错 token、完整命令名被过滤剔除且 Enter 只回填不执行、键盘导航更新错 state）→ 已修，完整命令 Enter 直接执行
  - 消息发送无反应：deepseek 报 `Invalid schema for function 'actor': got 'type: null'`——ToolJsonSchema.fromSchema 对顶层 Union 不补 type:object，openai-compatible 透传被 API 400 → json-schema.ts normalize 顶层 anyOf 全 object 时补 type:object（防递归守卫）；llm/tool.ts toJsonSchema 同步同规则；实测发消息正常返回推理+回复
- **新增对齐项**：
  - QueueDock 队列面板（useQueue 监听 prompt.admitted 入队 / prompted 出队 + QueueDock 组件）
  - 设置中心（SettingsModal：模型分组搜索 + 通用页）
- **验证**：webapp vitest 14 用例全绿；重建 dist + 重启服务（4096）页面 200
- **同步**：commit 27a0caa + push origin main，两端 HEAD 一致
- **剩余批次**：消息反馈（需后端接口）、三栏布局/会话树搜索分组、原生目录选择器、交付物文件引用、TTFT/吞吐、GoalBar、jobs 列表


---

## 三、gyc web 对齐 DSH 第六轮（2026-08-20 深夜）

- **会话树**：v1 session list 实测含 parentID（93 条中 24 条为子会话）→ useSessions 增 parentID 字段，SessionList 重写为树形渲染（子会话缩进 + 左缘线）
- **后台任务中心（对齐 DSH Jobs 面板）**：
  - 后端新增 `GET /experimental/jobs`：httpapi experimental group + handler 调 background.list()，返回 id/type/title/status/started_at/completed_at/output/error/metadata
  - 前端 `useJobs`：轮询 3s + session.busy/idle/updated 事件即时刷新
  - `JobsPanel` 折叠面板：状态点（运行中脉冲/失败红/取消灰）+ 标题 + 耗时；点击跳转任务会话；运行中可「取消」（v2 session.abort）
- **验证**：bun run build 通过；webapp vitest 8 文件 14 用例全绿；重启 4096 后 `GET /experimental/jobs` 实测返回 `[]`（无任务时），首页 200
- **同步**：commit a50b3f0 + push origin main，两端 HEAD 一致
- **剩余批次**：消息反馈（需后端接口）、三栏布局/会话树搜索分组、原生目录选择器、交付物文件引用、TTFT/吞吐、GoalBar
---

## 四、hermes agent 无法启动修复 + 升级至 0.20.4（2026-08-20）

### 现象
- 谷总反馈：本机 hermes agent 无法启动和使用（执行 `hermes` 报 `ENOENT ...assets/banner.txt`）。

### 排查与根因
1. `hermes` 命令被 npm 假包劫持：`C:\Program Files\nodejs\node_modules\hermes-cli`（巴西旅行社搜索工具 chrisenytc/hermes，2026-08-18 11:56 误装），运行即报 banner 缺失
2. 真身安装损坏：`AppData\Local\hermes\hermes-agent` 在 2026-08-18 22:52 升级时被标记为 `hermes-agent.broken-20260818-225249`，代码被移除、venv 残缺（仅剩 hermes.exe）；git 仓库 pack 损坏（1433 unresolved deltas，8-16 更新失败）
3. gateway 自 8-18 后停摆（最后一次心跳 8-18 00:06）；开机自启计划任务指向的 `Downloads\启动hermes-agent.bat` 已不存在

### 修复
1. 删除 npm 假包（nodejs 下 hermes/hermes.cmd/hermes.ps1/node_modules/hermes-cli）与系统 Python Scripts 旧 hermes.exe（v0.19.1 残留）
2. 通过 gh-proxy.com 代理 git clone 最新版到 `E:\myAI\hermes agent`（HEAD ad889540，v0.20.4 / 2026.8.18）
3. `uv venv venv --python 3.11` + `uv pip install -e .`（65 包）
4. 新增 `C:\Users\谷勇成\AppData\Local\hermes\bin\hermes.cmd/.ps1` 入口指向新版；HERMES_HOME 不变（AppData\Local\hermes，配置/密钥/微信/skills 全保留）
5. 重建 `Downloads\启动hermes-agent.bat`（指向新版 gateway start）；清理 PATH 失效条目与误装的 Hermes JS 编译器（hermesc.exe）

### 验证
- `hermes --version` → v0.20.4 (2026.8.18)；`hermes version` → Up to date
- `hermes status` → Gateway ✓ running（PID 12232），API 密钥/微信配置/session 全部保留
- `hermes chat -q` 真实对话返回「修复成功」（会话 20260820_085709_124d5c）
- 开机自启 bat 已重建，计划任务无需改动