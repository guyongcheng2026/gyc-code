# 2026-08-12 gyc-code 工作记录

> 类型：LLM 流空闲超时修复（Compose · DeepSeek V4 Flash 报错）
> 关联：[[2001.我的助手工具链/gyc-code-项目架构|gyc-code 项目架构]]
> 仓库：本地 `C:\Users\谷勇成\gyc-cli` / GitHub `guyongcheng2026/gyc-code`（commit `da08605`）

---

## 一、现象

- TUI / Compose 会话运行 DeepSeek V4 Flash 时报错：
  `LLM stream connection idle timeout: no data received within 300000ms. The connection may have dropped.`
- 会话中断，gyc cli 无法继续正常运行

## 二、排查结论

- 根因 1：`llm-timeout.ts` 流空闲超时默认 300s（5 分钟），DeepSeek V4 Flash（opencode 免费通道 / 代理排队）首 token 延迟或连接 hang 可超过该窗口
- 根因 2：超时错误消息 `connection idle timeout / no data received` 不匹配 `retry.ts` 的 `RETRYABLE_MESSAGE_PATTERNS`，被判为不可重试 → 直接失败终止，而非自动重试恢复

## 三、修复（commit `da08605`）

1. `src/gyccode/session/retry.ts`：`RETRYABLE_MESSAGE_PATTERNS` 增加 `idle` 关键词与 `/\bidle timeout\b|no data received/i`，空闲超时错误进入自动重试（最多 5 次，指数退避 2s→30s）
2. `src/gyccode/session/llm-timeout.ts`：默认 `LLM_STREAM_IDLE_TIMEOUT_MS` 300s → 600s（10 分钟），适配 DeepSeek V4 慢首 token
3. `src/core/v1/config/config.ts`：schema 描述默认值同步 600000
4. 新增 `retry.test.ts`（idle timeout 可重试 / 普通错误不可重试）；更新 `llm-timeout.test.ts` 断言
5. 用户配置 `~/.config/gyccode/gyccode.json` 增加 `llm.stream_idle_timeout_ms: 600000`（立即生效，无需等发布）

## 四、验证（全部通过）

- `bun test llm-timeout.test.ts retry.test.ts` 8/8 通过；date.test.ts 9/9 通过（附带提交遗留改动）
- `bun run build` 重建 dist；dist chunk 确认含 `600000` 默认值与 idle 重试模式
- `gyc --version` 正常输出 0.0.1

## 五、遗留事项

- 运行中的 gyc 实例需重启（重建后的 dist）后生效

---

## 六、全面稳定性排查（2026-08-12）

> 目标：发热 / 噪音 / 10 子代理并行 / 超时断 LLM / 24h 稳定运行

### 现象与证据

- 昨晚（08-11 23:14）日志：同一批 8 个并行子代理会话被 Abort，其中 1 个触发 `LLM stream connection idle timeout 300000ms`（旧版默认值），大量 summary 流报 `stream error`（provider=opencode，model=deepseek-v4-flash-free）
- 根因链：10 子代理并行 → 10+ 并发 LLM 流同时打到 opencode 免费通道 → 排队/限流 → 首 token 超时断流；同时并发叠加抬高 CPU 峰值（发热/噪音）
- 代码证据：`core/background-job.ts` 所有 job 无并发上限；task/swarm 无子代理数量上限（仅 depth 限制）；LLM 流无全局并发控制

### 修复（commit `4656ac6`）

1. `src/gyccode/session/llm.ts`：全局 `Semaphore` 并发闸，所有 LLM 流（主会话 + 子代理 + summary）最多 `max_concurrent_streams` 个并发，多余排队等待，不再并发轰炸 provider
2. `src/gyccode/session/llm-timeout.ts`：`LLM_MAX_CONCURRENT_STREAMS = 6` + `resolveMaxConcurrentStreams`
3. `src/core/v1/config/config.ts`：新增 `llm.max_concurrent_streams` 配置项（默认 6，可调）
4. 谷总 `gyccode.json` 已配置 `max_concurrent_streams: 6`

### 排查结论（无需改动）

- TUI 渲染循环：空闲时 live 计数归零自动暂停，无持续空转
- spinner：`animations_enabled` 默认关闭 → 静态符号，无动画开销；`BgPulse` 同样默认关闭
- 日志：已有 10MB 轮转（gyccode.log → .log.1）+ 串行写队列
- watcher：@parcel/watcher + ignore 模式，仅监听工作目录
- tokenizer WASM：仅 compaction/记忆提取时调用，非每 token 热点
- WebSocket（openai responses）有独立 idle timeout 保护；谷总走 SSE 非该路径

### 验证（全部通过）

- `bun test`：llm-timeout + retry 11/11；session 目录 191/191
- 运行时：日志输出 `llm concurrency limit maxStreams=6`；空闲 serve 进程 5s CPU 增量 0.00s，内存 29.4MB
- `gyc --version` 正常


---

## 七、架构审查 ROUND3：六问题深挖与对标落地方案（2026-08-12 下午）

> commit `8300e4f`；报告：`docs/compose/reports/2026-08-12-arch-round3.md`
> 目标：发热/噪音/幻觉率/缓存命中率/启动包/存储数据库六类问题根因重审，对标 Claude Code + i5-5250U 硬件约束找最佳方案

### 深挖发现（本机实测）

1. **DB 膨胀治本**：DB 182.3MB，event 表 `message.part.updated` 39821 行/98.5MB，其中 **tool 类 25981 行/91.1MB** —— v1 路径每个 tool 经历 pending→running→completed 三次落库，每次都写完整 part 快照（含大 output）
2. **幻觉防护缺口**：15 份提示词仅 4 份有防幻觉段落，**default.txt（主力 deepseek 走 default）等 6 份完全缺失**
3. **冷启动瓶颈**：bin/gyc 用 node spawnSync bun = 双进程三层链（bun shim→node→bun），`gyc --version` 2.90s（实测分解：node 0.33 + bun 0.58 + 模块加载 0.75）
4. **日志噪音**：provider 60s 超时/限流时同 run 每步重试 3 次全量 ERROR 落盘，日志 12.8MB
5. **缓存 0%**：已闭环确认是免费模型不支持 prompt caching（非代码缺陷），promptCacheKey 已全 provider 覆盖

### 落地改动（commit 8300e4f）

1. `session.ts` 新增 `updatePartLive`：中间态 publishLive（TUI 实时可见）+ 直投影 part 表（运行中读取一致），**不写 event 表**；仅最终态走 durable —— tool 事件 3→1 条/次，text/reasoning 2→1 条/次，对标 CC「只持久化最终消息」
2. `processor.ts`：text-start/reasoning-start/tool pending+running 共 5 处改 updatePartLive
3. 提示词补防幻觉段落（# Accuracy and honesty，与 beast/gpt 一致）：default/anthropic/gemini/kimi/meta/codex 共 6 份，全模型覆盖
4. `bin/gyc` 改 bun shebang 直跑（bun 下同进程 import dist，node 兼容回退保留）→ 冷启动 **2.90→1.26s**（热 0.86s）
5. `logging.ts`：ERROR 60s 同类限流（同 run+同 message 合并）
6. 数据层：`maintain-data --prune-events=8 --clean --checkpoint` + VACUUM → **DB 182.3→136.0MB**（回收 46MB）

### 验证（全部通过）

- tsc --noEmit 0 错误；bun test 375 pass / 0 fail；bun run build done
- gyc --version 1.26s（工作区）/1.98s（跨目录首次），低于 3.5s 达标线
- DB integrity_check OK

### 遗留待决策

- v1/v2 执行器统一后事件路径仅剩 v2，DB 模型可再瘦身
- 换支持缓存模型验证命中率（`gyc db cache` 闭环已就绪）
- bun compile 单文件分发 vs 依赖剪枝（dist 232MB → ≤150MB 目标）
- maintain-data 建议纳入 Windows 计划任务定期自动执行

---

## 八、GitHub/知识库连接 + 遗留改动同步（2026-08-12 15:08）

### 连接验证

- GitHub（gyc-cli，gh-proxy 代理）：`git ls-remote` 连通正常，本地与远程 main 一致
- 知识库（Gitee）：连通正常，本地与远程 main 一致（7ba94b3），工作区干净

### 同步提交（commit 序列）

1. `b3ad5cc` UI调整：工作区 Context 摘要从首页移至会话侧边栏展示（home.tsx 删 WorkspaceContextSummary / sidebar.tsx 新增）
2. `463fd17` 配置：LLM 流新增 `first_token_timeout_ms` 首 token 超时配置，空流快速失败不再阻塞主循环（llm-timeout.ts / llm.ts / config.ts + 测试）
3. `795db12` 清理：移除误提交的 `.temp-stab.cjs` 临时调试脚本（263 行），`.gitignore` 增加 `.temp-*.cjs/.temp-*.js` 规则

### 验证

- 两端一致：本地 HEAD = 远程 main = `795db12`，`git status -sb` 无 ahead/behind
- 后续遗留改动同步：架构 ROUND4 审查前先同步当日待提交改动

---

## 九、稳定性修复：LLM provider 超时导致 runLoop 静默阻塞数分钟（2026-08-12 晚间）
> commit `e3546d8`；现象：LLM 运行几分钟后失去响应，需要重发消息才能恢复

### 现象与证据
- 日志（`~/.local/share/gyccode/log/gyccode.log`）：provider（nvidia/nemotron）反复 `ProviderHeaderTimeoutError: Provider response headers timed out after 60000ms`，每次重试等 60s 后再发起，循环 5+ 次，runLoop 静默阻塞数分钟，期间 TUI 无响应

### 根因链路
- `processor.ts`：`llm.stream` 的 `Stream.runDrain` 阻塞；外层 `Effect.retry` 最多 5 次
- `retry.ts`：`HeaderTimeoutError` 被标记 `isRetryable`；重试单次最长 60s、无总时长上限，60s×5 约 6 分钟静默
- 并发闸（`Semaphore` 6）已确认正常，非并发问题

### 修复（commit `e3546d8`）
1. `retry.ts`：新增 `RETRY_TOTAL_CAP_MS = 120_000`，重试累计耗时超 2 分钟立即放弃并报错，不再静默空等
2. `llm-timeout.ts`：新增 `LLM_FIRST_TOKEN_TIMEOUT_MS = 180_000` + `resolveFirstTokenTimeout` + `withFirstEventTimeout`（`Stream.toPull` + `Effect.timeout` 首帧快速失败，成功后重放首帧续拉）
3. `llm.ts`：AI SDK 转换流外层包 `withFirstEventTimeout`，再套 `streamWithIdleTimeout`（idle 超时仍负责流中段停滞）
4. 配置：`llm.first_token_timeout_ms` 可调（默认 180000，配置项提交 `463fd17`）

### 验证（全部通过）
- `tsc --noEmit` 0 错误；`bun test` 381 pass / 0 fail（基线 375 + 新增 6）；`bun run build` done
- GitHub 已同步：本地 HEAD = 远端 main = `e3546d8`

### 后续待观察
- 观察 nvidia/nemotron 等慢 provider 是否仍触发重试；如 180s 首事件超时仍偏短，可调 `first_token_timeout_ms`

---

## 十、opencode 依赖剥离：SDK/plugin 客户端本地化（纯自研化第一阶段，2026-08-12 深夜）
> commit `c8e4dad`；目标：gyc-cli 不再依赖任何 `@opencode-ai/*` 外部包，代码层实现技术自研化

### 背景
- 评估发现 gyc-cli 为 opencode 1.18 派生（8.7 万行 vendored 内核 + 8.9 万行自研层），外部仅 2 个 opencode 直接依赖
- 纯自研分四层：L1 品牌（基本完成）、L2 依赖剥离（本轮）、L3 内核重写（不划算，否决）、L4 法律层（MIT 铁律不可行）
- 本轮目标 = L2：bun.lock 无 opencode、源码无 @opencode-ai 运行时依赖

### 改动清单
1. **新增 `src/protocol/v2/`**：原 `@opencode-ai/sdk/v2` 本地化——`gen/types.gen`（11.6k 行类型）+ `gen/sdk.gen`（方法类）+ `gen/client`、`gen/core`（hey-api 运行时 1.1k 行，零外部依赖）+ 自研 `createGyccodeClient` 封装（fetch/拦截器/directory 头重写）
2. **新增 `src/protocol/v1/`**：原 `@opencode-ai/sdk`（非 v2 兼容入口）本地化
3. **新增 `src/protocol/plugin/`**：原 `@opencode-ai/plugin` 包 68 文件 vendored（MIT 保留）
4. **移除 5 个依赖**：`@opencode-ai/sdk`、`@opencode-ai/plugin`、`@gitlab/opencode-gitlab-auth`、`opencode-gitlab-auth`、`opencode-poe-auth`
5. **删除 Gitlab/Poe 认证插件**（gitlab 走独立 `gitlab-ai-provider` + PAT 路径，不受影响）
6. **类名统一**：`OpencodeClient` → `GyccodeClient`；函数 `createOpencodeClient` → `createGyccodeClient`；header `x-opencode-*` → `x-gyccode-*`；错误文案 "opencode server" → "gyc server"
7. **protocol exports 扩展**：`./v1`、`./v2`、`./plugin` 及子路径映射
8. **脚手架文案**：插件生成模板改为安装 `@gyccode/plugin`

### 验证（全部通过）
- `bun run build` done；`bun test` 395 pass / 0 fail
- `bun run dev -- --help` 正常出 CLI
- `bun.lock` 无 opencode；`package.json` 无 opencode；源码仅 v1/v2/index.ts 注释披露来源
- GitHub 已同步：本地 HEAD = 远端 main = `c8e4dad`

### 遗留（阶段 3 品牌清理，后续处理）
- 约 30 处 `opencode.ai` 外部 URL（HTTP-Referer / config schema / models 源等），需决策替换目标域名
- `.opencode` skill 兼容目录（功能保留，属生态兼容特性）
- vendored 类型注释中的 "opencode" 字样（保留，MIT 披露）
